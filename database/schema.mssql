DROP DATABASE IF EXISTS tweetDB;

CREATE DATABASE tweetDB;

USE tweetDB;

DROP TABLE IF EXISTS tweets;
		
CREATE TABLE tweets (
  id INTEGER AUTO_INCREMENT,
  created_at DATETIME NOT NULL,
  text VARCHAR(140) NOT NULL,
  user_id VARCHAR(140) NOT NULL,
  PRIMARY KEY (id)
);

ALTER TABLE tweets AUTO_INCREMENT=1;

#LOAD DATA LOCAL INFILE 'database/data.txt' INTO TABLE tweets FIELDS TERMINATED BY '\t' LINES TERMINATED BY '\n' IGNORE 1 LINES (created_at, text, user_id);